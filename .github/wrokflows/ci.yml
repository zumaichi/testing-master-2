name: CI workflow
on: pull_request
jobs:
  ci-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: CheckoutRepository
        uses: actions/checkout@v6
      - name: install
        run: npm ci
      - name: test
        run: npm test
